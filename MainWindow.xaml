<Window x:Class="WpfDimensionSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:WpfDimensionSample.Controls"
        Title="寸法連携サンプル"
        Width="1200"
        Height="600"
        MinWidth="700"
        MinHeight="500">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Padding="18"
                Background="#FFF7F9FC"
                BorderBrush="#FFD9E2EC"
                BorderThickness="1"
                CornerRadius="6">
            <StackPanel>
                <TextBlock Margin="0,0,0,8"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Text="図形パラメータ" />

                <TextBlock Margin="0,8,0,4" Text="図形パターン" />
                <ComboBox ItemsSource="{Binding Patterns}"
                          SelectedItem="{Binding SelectedPattern}"
                          DisplayMemberPath="Name" />

                <Separator Margin="0,18,0,12" />

                <ItemsControl ItemsSource="{Binding SelectedPattern.InputRows}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,12"
                                  Visibility="{Binding IsVisible,
                                                       Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock VerticalAlignment="Center" Text="{Binding Label}" />
                                    <CheckBox VerticalAlignment="Center"
                                              Margin="4,0,0,0"
                                              IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                              Visibility="{Binding IsCheckBoxVisible,
                                                                   Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </StackPanel>
                                <ItemsControl Grid.Column="1" ItemsSource="{Binding Slots}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal"
                                                        Visibility="{Binding IsVisible,
                                                                             Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock VerticalAlignment="Center"
                                                           Margin="0,0,4,0"
                                                           Text="{Binding Prefix}" />
                                                <TextBox Width="54"
                                                         Margin="0,0,4,0"
                                                         Padding="5,3"
                                                         IsEnabled="{Binding DataContext.IsSlotsEnabled,
                                                                             RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                         Text="{Binding Parameter.ValueText,
                                                                        UpdateSourceTrigger=PropertyChanged,
                                                                        ValidatesOnNotifyDataErrors=True}" />
                                                <TextBlock VerticalAlignment="Center"
                                                           Margin="0,0,6,0"
                                                           Text="{Binding Suffix}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Margin="0,10,0,0"
                           Foreground="#FF52606D"
                           TextWrapping="Wrap"
                           Text="値を変更すると、図形と寸法線が即座に更新されます。" />
            </StackPanel>
        </Border>

        <Border Grid.Column="2"
                Background="White"
                BorderBrush="#FFD9E2EC"
                BorderThickness="1"
                CornerRadius="6">
            <controls:DimensionDrawingView Drawing="{Binding Drawing}" />
        </Border>
    </Grid>
</Window>
